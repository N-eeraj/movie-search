const Loading = ({ className }) => {
  return (
    <div className={`aspect-square border-2 border-accent border-b-light-extra/50 rounded-full animate-spin ${className}`} />
  )
}

export default Loading
